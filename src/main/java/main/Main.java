package main;

public class Main {

    //TODO
    /* - webpage with a form (value + "from unit" + "to unit" dropdowns
       - Submitting the form sends data to the server
       - Server calculates the result and re-renders the same page with the answers shown
       - old-school "submit and reload" pattern, not a modern SPA(?)
     */
}
